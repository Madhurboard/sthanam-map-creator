# Base URL of the Sthanam backend (payments + download tokens).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Development only: render paid tiers without going through Razorpay.
# Ignored in production builds. Leave unset unless you are testing exports.
# NEXT_PUBLIC_SKIP_PAYMENT=1
